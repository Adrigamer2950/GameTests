//file:noinspection GroovyAssignabilityCheck
plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.9.0"
}

rootProject.name = 'GameTests'

["paper"].forEach {
    include("$it")
    project(":$it").projectDir = file(it)
}
